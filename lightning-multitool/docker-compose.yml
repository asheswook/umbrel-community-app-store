version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: ${APP_LMT_GUI_IP}
      APP_PORT: 3030

  gui:
    image: asheswook/lightning-multitool-umbrel:v0.0.11
    restart: on-failure
    volumes:
      - lmt-data:/app
    environment:
      LMT_CONF_PATH: /app
      LMT_CORE_URL: "http://${APP_LMT_IP}:${APP_LMT_API_PORT}"
    networks:
      default:
        ipv4_address: ${APP_LMT_GUI_IP}

  core:
    image: ghcr.io/asheswook/lightning-multitool:v0.1.13-beta@sha256:5b93530b2d84a9de00498534524ea5efef2d235bc2ea5388fe9475a4d84bddf0
    user: root
    deploy:
      restart_policy:
        condition: any
        delay: 5s
    init: true
    volumes:
      - ${APP_LIGHTNING_NODE_DATA_DIR}:/root/.lnd:ro
      - lmt-data:/app
    entrypoint:
      - sh
      - -c
      - /usr/local/bin/lmt
    environment:
      LND_HOST: ${APP_LIGHTNING_NODE_IP}:${APP_LIGHTNING_NODE_REST_PORT}
    ports:
      - ${APP_LMT_API_PORT}:${APP_LMT_API_PORT}
      - ${APP_LMT_PORT}:${APP_LMT_PORT}
    networks:
      default:
        ipv4_address: ${APP_LMT_IP}

volumes:
  lmt-data: